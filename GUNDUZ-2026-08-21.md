# Gündüz Çalıştırması — 21 Ağustos 2026 (09:30 – 10:52)

`GECE-2026-08-21.md`'deki "0 alım" teşhisinin düzeltilmesi ve yeniden ölçüm.
Üç tur koşuldu; ilk ikisi ölçüm değil **hata teşhisi** üretti.

## Gece raporunun iki teşhisi doğruydu ama eksikti

Rapor iki kök sebep saptamıştı (`analyze_delay=40`, hacim kuralının kapanmaması).
İkisi de gerçekti ve düzeltildi. Ama tek başlarına "0 alım"ı açıklamıyorlardı:
**gece turunda alım zaten imkânsızdı.**

## Bulunan 5 kök sebep

### 1. `analyze_delay = 40` çok erken  [gece raporundan]

40. saniyede coinlerin %95'i launch tabanında; filtre kötü coin değil henüz
doğmamış coin eliyordu. `backend/state.py` varsayılanı 90.0 yapıldı.
`settings.json` `.gitignore`'da olduğu için **kod varsayılanı tek gerçek kaynak.**

### 2. Hacim kuralı kapanmıyordu  [gece raporundan]

`min_volume_5m = 0` "kapat" niyetiydi ama karşılaştırma `<=` olduğu için hacmi
sıfır olan her coini eliyordu (gecede 4661 kararın 4406'sı). Artık eşik pozitif
değilse kural hiç uygulanmıyor.

| `volume` kuralı | gece | gündüz 3. tur |
|---|---|---|
| devrede | 4406 / 4661 (%94,4) | 1 / 142 (%0,7) |

### 3. `strategy_mode: "sniper"` diye bir mod yok  [YENİ — asıl sebep]

Geçerli modlar `copy` ve `legacy`. Bilinmeyen modda `legacy_sniper` alımı
"arşivde" deyip atlıyor (`legacy_sniper.py:122`), `copy_engine` de `mode != copy`
olduğu için boşta duruyor. Gece raporunun ayar tablosunda `strategy_mode sniper`
yazıyor: **o turda eşikler ne olursa olsun alım yapılamazdı.**

`state.STRATEGY_MODES` eklendi; `update()` bilinmeyen modu loglayıp yoksayıyor.

### 4. Eksik curve verisi = sessizce kontrol yok  [YENİ]

`read_curve` hem RPC hatasında hem coin DEX'e geçtiğinde `None` dönüyor ve
`curve_complete` yalnızca curve okunabildiğinde set ediliyor — iki durum ayırt
edilemiyordu. Sonuç: 429 yiyen coin `curve_low`/`curve_high`'ı hiç görmeden
geçiyordu. `The Boo Dog` (`curve_sol=None`) böyle alındı, **95 saniyede -%67**.

Artık eksik curve = `no_curve` = FAIL. Curve dolmuş (DEX'e geçmiş) coinde
eşikler zaten anlamsız olduğu için hacme bakılıyor.

### 5. Giriş fiyatı bayattı  [YENİ — LIVE modda doğrudan para kaybı]

Fiyat `collect_metrics`'in ilk işleminde alınıyor; arkasından gelen derin analiz
(holder + erken alıcı taraması, yoğun launch'ta 100+ `getTransaction`) rps tavanı
ve 429 beklemeleriyle dakikalarca sürebiliyor. `price_hint` doğrudan giriş fiyatı
sayılıyordu.

`KRANK` alımından **2 saniye** sonra -%31,6 stop loss yedi: hint gerçek fiyatın
%44 üstündeydi. `trader.buy` artık alım anında taze fiyat okuyor, hint yalnızca
yedek, %10+ sapmada uyarı logluyor.

## Yan bulgu: 429 seli

`.env`'de `RPC_RPS` yoktu, kod varsayılanı 9 ile koşuldu. 1. turda 7 dakikada
~150 adet 429. `.env.example` zaten 6.5 öneriyor — ve `ACIK-ISLER.md`'ye göre bu
karar **19 Ağustos'ta verilmiş ama koda hiç işlenmemiş.**

Kod varsayılanı 6.5 yapıldı. `/api/status` artık `rpc` sayaçlarını döndürüyor
(429'lar loga 25'te bir yazıldığı için şiddet hiçbir yerden okunamıyordu).

## Kalıcı ders

Bu projede kararlar belgeye yazılıp koda işlenmiyor: `analyze_delay=90` ve
`RPC_RPS=6.5` ikisi de 19 Ağustos'ta karara bağlanmış, ikisi de kod varsayılanına
girmemişti. `settings.json` gitignore'da olduğu için kod varsayılanı tek gerçek
kaynak. İkisi için de regresyon testi yazıldı.

## Testler

`tests/test_rules.py` eklendi (kural motorunun eşik davranışı, ağ erişimi yok).
`pytest tests/` 63 test + `tools/offline_test.py` tamamı geçiyor.

`tools/offline_test.py` iki kez kırıldı ve ikisi de testin kendi hatasıydı:
yerel `settings.json`'daki `auto_buy_sol`'a bağlıydı, ve `buy()` artık
`current_price` çağırdığı için gerçekten offline değildi. İkisi de düzeltildi.

---

# ÖLÇÜM — 3. tur (10:22:05 – 10:53, ~31 dk)

Tüm düzeltmeler devrede, paper cüzdan sıfırlanmış ($200 / 2.2009 SOL),
`strategy_mode=legacy`, `analyze_delay=90`, alım 0.1 SOL.

| ölçüm | gece (8 sa) | gündüz 3. tur (31 dk) |
|---|---|---|
| görülen coin | 4518 (~9,4/dk) | 258 (~8,3/dk) |
| karar | 4661 | 259 |
| **alım** | **0** | **5 (%1,9)** |
| `volume` kuralı devrede | 4406 (%94,4) | **0** |
| `mcap_high` devrede | 20 (%0,4) | 11 (%4,2) |

Coin keşif hızı gece ile aynı (~8-9/dk), yani gündüz turu temsil edici.

## Kural dağılımı (259 karar)

| kural | devrede | tek sebep |
|---|---|---|
| `mcap_low` | 210 | 6 |
| `curve_low` | 168 | – |
| `bundler` | 116 | 1 |
| `top10` | 59 | **12** |
| `no_curve` | 46 | 5 |
| `sniper` | 30 | 1 |
| `mcap_high` | 11 | – |
| `dev` | 8 | – |
| diğer eksik veri (`no_*`) | 8 | – |

`sole_blocker` toplamı 26 — gecedeki 66'ya göre oransal olarak çok daha yüksek
(%10 vs %1,4), yani filtre artık "hepsi birden takılıyor" halinde değil.
**`top10` en çok tek-sebep olan kural (12).** Bir sonraki tur bu eşiği
(`max_top10=25`) sorgulamalı.

## Sonuç (paper)

| | |
|---|---|
| kapanan işlem | 1 → `The Odyssean Owl` -%33,2 (-0,0367 SOL), gerçek piyasa hareketi |
| açık pozisyon | 4 → `AVA FONTAINE` +%22,9, diğer üçü -%0,8 / -%1,5 / -%9,1 |
| açık gerçekleşmemiş | +0,0115 SOL |

Tek kapanan işlem tek başına anlamlı değil; ama gece turunun **hiç** üretemediği
veri artık üretiliyor.

## Yan ölçüm: 429 hâlâ yüksek ama zararsız

`rpc: {calls: 8416, rate_limited: 902, failed: 0}` → %10,7 oranında 429, ama
**başarısız çağrı sıfır**: retry'lar yutuyor.

Oran tur boyunca %1,6'dan %10,7'ye çıktı; açık pozisyon takibi (`monitor_loop`)
analizle aynı RPC havuzunu paylaşıyor, yani **alım arttıkça analiz verisi
baskılanıyor.** `no_curve` da %17,8 (46 coin) — bunun ne kadarı rate limit, ne
kadarı pump.fun dışı launchpad / DEX'e geçmiş coin, ayrıştırılmadı.

Çözüm rps'i daha da düşürmek değil, pozisyon fiyatlarını Dexscreener'dan çekmek
(ücretsiz, ayrı rate limit) veya takip ile analize ayrı bütçe vermek olmalı.
Bu tura dahil edilmedi.

## Sıradaki

1. `max_top10=25` sorgulanmalı — 12 coini tek başına eleyen kural bu.
2. Pozisyon takibi ile analiz için ayrı RPC bütçesi.
3. `trailing_stop=30`: 1,43x altındaki her tepe için trailing, girişin altına
   düşüyor (`Beef & Cow Index` 2. turda +%40'tan -%2,3'e döndü). Kademeli
   satışın ilk basamağı ile trailing'in silahlanma noktası birlikte ele alınmalı.
4. Uzun tur (gece) — ama artık ölçüm değeri olan bir turla.

# Gece Çalıştırması — 21 Ağustos 2026 (00:02 – 08:00, ~8 saat)

Sniper hattı, PAPER modu, `e710398`'in düzeltilmiş eşikleriyle.

## Sonuç: 4661 coin analiz edildi, **0 alım**

Bakiye ($200 / 2.2823 SOL) hiç kullanılmadı. PnL verisi **yok** — bu turdan
kâr/zarar hakkında hiçbir şey öğrenilmedi. Öğrenilen şey filtre tarafında.

| ölçüm | değer |
|---|---|
| görülen coin | 4518 |
| kaydedilen karar | 4661 (hepsi RED) |
| alım | 0 |
| hata / exception / rate-limit | 0 |
| çalışma | kesintisiz, websocket hiç kopmadı |

## Kural dağılımı

| kural | devrede | tek sebep (`sole_blocker`) |
|---|---|---|
| `mcap_low` | 4463 (%95.7) | 19 |
| `volume` | 4406 (%94.4) | 14 |
| `curve_low` | 3884 (%83.3) | – |
| `bundler` | 2281 (%49.3) | 4 |
| `top10` | 604 (%13.1) | 28 |
| `sniper` | 540 (%11.6) | – |
| `mcap_high` | 20 (**%0.4**) | 1 |

`sole_blocker` toplamı 4661 kararda yalnızca **66**. Yani coinlerin ezici
çoğunluğu aynı anda 3-4 kurala birden takılıyor; **tek bir eşiği gevşetmek
anlamlı sayıda coin serbest bırakmaz.**

## Teşhis 1 — `analyze_delay = 40` çok erken (ASIL SEBEP)

Reddedilen coinlerin ham değerleri:

```
FUCK U CENTE   mcap $2489   curve 0.25 SOL   vol5m 0
CHECK TWEET    mcap $2448   curve 0.00 SOL   vol5m 0
ga             mcap $2448   curve 0.00 SOL   vol5m 0
K-Stack        mcap $2460   curve 0.00 SOL   vol5m 0
```

$2448–2489 coinin gerçek değeri değil, **bonding curve'ün launch anındaki
matematiksel tabanı**. 40. saniyede coinlerin %95'i henüz hiç işlem görmemiş.
Filtre kötü coin elemiyor — **daha doğmamış coine bakıyor**.

`ACIK-ISLER.md`'deki eski kalibrasyon bunu zaten ölçmüştü:

> 30 saniyede coinlerin neredeyse tamamı launch tabanında duruyordu
> (mcap $2281, curve 0.00 SOL); 90 saniyede ayrışıyorlar
> ($2281 → $34.436, curve 0 → 0.88+).

`e710398` bu ayarı 90 → 40 düşürdü. Gece verisi bu değişikliğin **yanlış**
olduğunu gösteriyor.

## Teşhis 2 — ters seçim hipotezi ÇÜRÜDÜ

`ANALIZ-BRIEF.md` madde A: *"`max_mcap $25k` tavanı runner'ları kesiyor,
filtre ilgi görmemiş coin seçiyor."* Tavan 25k → 150k açıldı ve
**4661 coinde sadece 20 kez devreye girdi (%0.4)**.

Darboğaz tavanda değil **tabanda**. Bu artık çıkarım değil, ölçüm.
Brief'in 1. ve 2. sorusu bu veriyle kapandı.

## Teşhis 3 — GERÇEK BUG: hacim kuralı kapanmıyor

`backend/analyzer.py:637-640`

```python
if metrics.volume_5m is not None and metrics.volume_5m <= cfg.min_volume_5m:
    add("volume", ...)
```

`min_volume_5m = 0` "bu kuralı kapat" niyetiyle ayarlanmıştı (bkz.
`ACIK-ISLER.md` madde 4). Ama `0 <= 0` doğru olduğu için hacmi sıfır olan
her coin eleniyor. Kural kapalı sanılıyordu — **4406 kez tetiklendi.**

---

# DÜZELTİLECEKLER

## 1. `analyze_delay` 40 → 90  [öncelik: yüksek]

Ölçülmüş değer, tahmin değil. **Not:** `settings.json` `.gitignore`'da, yani
bu repoyu klonlayan makinede yok — kod varsayılanı geçerli olur:

- `backend/state.py:99` → `analyze_delay: float = 40.0` → **90.0**

Sadece panelden/`settings.json`'dan değiştirmek yetmez, kod varsayılanı da
düzeltilmeli.

## 2. Hacim kuralındaki `<=` → `<`  [öncelik: yüksek]

`backend/analyzer.py:637` ve `:639`. Ya karşılaştırmayı `<` yap, ya da
daha açık olanı: `min_volume_5m <= 0` ise kuralı tamamen atla.
Eşiği 0'a çekerek kapatma niyeti şu an çalışmıyor.

## 3. `min_mcap $5000` — şimdilik DOKUNMA

90 saniyede coinler $34k bandına çıkıyorsa bu eşik zaten geçilir.
Önce (1) düzeltilip yeniden ölçülmeli. `analyze_delay` düzelmeden bu eşiği
gevşetmek, doğmamış coinleri satın almaya başlamak demek.

## 4. Yeniden ölçüm — gece beklemeye gerek yok

pump.fun gündüz de aynı hızda coin doğuruyor. (1) ve (2) uygulandıktan sonra
20–30 dakikada ilk kararlar okunabilir; alım gelip gelmediği hemen görülür.

## 5. Küçük / ertelenebilir

- Sniper modunda copy engine loop'u boşuna dönüyor, 2 dakikada bir
  `"Takip edilen cuzdan yok"` logluyor. Zararsız gürültü.
- Python logging stderr'e yazıyor; `Start-Process -RedirectStandardOutput`
  ile başlatılırsa asıl log `.err.log` dosyasında oluyor.

---

## Bu turda yapılan kod değişikliği

`tools/offline_test.py` — "asiri yogun launch -> RED" testi bayattı.
`e710398` `reject_on_busy_launch`'ı varsayılan olarak kapatmıştı (yoğun launch
penceresi runner'ın imzasıdır), test hâlâ eski koşulsuz-red davranışını
bekliyordu. Test yeni davranışa güncellendi ve kural *açıkken* hâlâ
reddettiğini de doğruluyor. `tools/offline_test.py` ve `pytest tests/`
(56 test) tamamen geçiyor.

## Loglar

- `logs/2026-08-21-gece.log` — gecenin tam logu (~890 KB, 13k satır)
- `logs/2026-08-21-coins-stats.json` — `/api/coins/stats` ham çıktısı
- `logs/2026-08-20-ilk-oturum.log` — ilk kısa oturum

## Çalıştırma ayarları (bu turda kullanılan)

```
strategy_mode   sniper        paper_trading   true
analyze_delay   40  (<- SORUN)  auto_buy_sol    0.1 SOL
max_mcap        150000        paper bakiye    2.2823 SOL ($200)
max_curve_sol   70            min_mcap        5000
min_curve_sol   2.0           min_volume_5m   0  (<- BUG, kapanmıyor)
reject_on_busy_launch  false
```

Ücret notu: `fee_priority_sol = 0.001` sabit. 0.01 SOL pozisyonda gidiş-dönüş
maliyet ~%21, 0.1 SOL'da ~%7. Bu turda 0.1 SOL kullanıldı.
